/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.freepik.com','images.unsplash.com'], // Add the domain from which you want to load images
    },
  }
  
  export default nextConfig;
  